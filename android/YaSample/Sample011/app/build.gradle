apply plugin: 'com.android.application'

android {
    compileSdkVersion 13
    buildToolsVersion "25.0.3"

    defaultConfig {
        applicationId "ya.Sample011"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
